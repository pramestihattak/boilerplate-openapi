linters:
  enable:
    - errcheck
    - gosec
    - staticcheck
    - govet

linters-settings:
  gosec:
    excludes:
      - G401
      - G501

issues:
  exclude-rules:
    - path: "_test\\.go"
      linters:
        - gosec
    - path: "mock/"
      linters:
        - all
